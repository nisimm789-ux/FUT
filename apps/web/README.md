# apps/web (placeholder)

Intentionally empty in Phase 0.

The product experience lives **inside/alongside the EA FC Web App** (extension
in-page control + side panel). This app will later host only:

- landing / marketing pages
- account & billing
- admin (remote config, SAFE_MODE switches, adapter compatibility dashboard)
- documentation

It must not grow into a replacement for the EA Web App.
